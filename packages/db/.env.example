# Local development and migration target. Any PostgreSQL works; production is
# a Supabase project whose connection string lives only in the Cloudflare
# Hyperdrive configuration, never in this repository.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/elafda
