## Tasks

1. Confirm the Windows failure mode in the current `apps/web/package.json` scripts and document the affected commands.
2. Replace POSIX inline environment assignments with a cross-platform equivalent that retains the current environment variables.
3. Install the required package and update the lockfile without changing application logic.
4. Validate the affected commands with the project’s existing build/test workflow.
5. Confirm the fix leaves the Unix and CI workflow behavior unchanged.
